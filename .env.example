# Didar Gold Platform - Environment Configuration Template
# Only true secrets or optional external keys are declared here.
# Default internal configs (ports, hosts, local DBs) are handled automatically in code.

# (Optional) AI Assistance API Key
GEMINI_API_KEY=
